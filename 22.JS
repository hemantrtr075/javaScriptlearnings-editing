// intro to for loop 
// prin t 0 to 9

// if let variable declared inside the loop then we 
// cannot print it outside the loop and opposite 
// we can print the var variable even it is declared 
// inside the forloop

for (let i =0; i<=9; i++){
    console.log(i);
}
// console.log(`"value of "${i}`);